CREATE TABLE user (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    first_name TEXT,
    last_name TEXT,
    avatar_url TEXT
);

selectAll:
SELECT *
FROM user;

insert:
INSERT INTO user(first_name, last_name, avatar_url)
VALUES (?, ?, ?);

delete:
DELETE
FROM user
WHERE id = ?;
