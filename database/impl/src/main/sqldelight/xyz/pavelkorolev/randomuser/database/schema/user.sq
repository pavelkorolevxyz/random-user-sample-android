CREATE TABLE user (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    first_name TEXT,
    last_name TEXT
);

selectAll:
SELECT *
FROM user;

insert:
INSERT INTO user(first_name, last_name)
VALUES (?, ?);
