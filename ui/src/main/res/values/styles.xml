<resources>

    <style name="Theme.RandomUser" parent="Theme.MaterialComponents.DayNight">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondaryDark</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnError">@color/white</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/primaryDark</item>
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <style name="Widget.RandomUser.Toolbar.Top" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:theme">@style/ThemeOverlay.RandomUser.Toolbar.Top</item>
        <item name="titleTextColor">?colorOnPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="ThemeOverlay.RandomUser.Toolbar.Top" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary" />

</resources>
